name: richards-coffee-shop
services:
  discord-music-bot:
    build: .
    ports:
      - '4242:4000' # Heartbeat port
    restart: always
    volumes:
      - music-cache:/usr/src/app/cachedir

volumes:
  music-cache:
